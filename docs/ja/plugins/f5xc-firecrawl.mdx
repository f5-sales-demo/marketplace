---
title: f5xc-firecrawl
description: オープンソースの Firecrawl エンジンを使用したローカルセルフホスト型のウェブスクレイピングおよびクローリング。
sidebar:
  order: 13
i18n:
  sourceHash: a9d67be9cf3e
  translator: machine
---

import { Aside, Badge } from '@astrojs/starlight/components';

**f5xc-firecrawl** プラグインは、オープンソースの [firecrawl](https://github.com/mendableai/firecrawl) エンジンを使用したローカルセルフホスト型のウェブスクレイピングを提供します。APIキー不要、サブスクリプション不要、クラウド依存なし。すべての操作は、開発コンテナ内の `localhost:3002` 上のローカル firecrawl インスタンスに対して実行されます。

<Badge text="v1.1.0" variant="note" /> <Badge text="Productivity" variant="tip" />

## インストール

```
/plugin install f5xc-firecrawl@f5xc-salesdemos-marketplace
```

## コマンド

### /scrape

単一の URL をスクレイピングし、コンテンツを Markdown として抽出します。

```
/scrape https://docs.example.com/getting-started
/scrape https://example.com --format markdown,links --wait 2000
```

### /batch-scrape

複数の URL を一度にスクレイピングします。

```
/batch-scrape https://example.com https://example.org https://example.net
```

### /crawl

開始 URL から複数のページをクローリングします。

```
/crawl https://docs.example.com --limit 20 --depth 2
/crawl https://docs.example.com --include /api/* --exclude /blog/*
```

### /map

ウェブサイト上のすべての URL を検出します。

```
/map https://docs.example.com
/map https://docs.example.com --search api --subdomains
```

### /search

ウェブを検索し、必要に応じて結果をスクレイピングします。

```
/search "firecrawl web scraping" --limit 10
/search "AI tools 2026" --scrape --time month
```

### /extract

LLM を活用したウェブページからの構造化データ抽出。

```
/extract https://example.com "Extract the main heading and any links"
/extract https://example.com/pricing --schema '{"plans": [{"name": "string", "price": "string"}]}'
```

<Aside type="note">
  extract コマンドを使用するには、`OPENAI_BASE_URL` および `OPENAI_API_KEY` で設定された OpenAI 互換の LLM プロキシが必要です。
</Aside>

### /llmstxt

サイトの llms.txt ファイルを生成します。

```
/llmstxt https://docs.example.com
```

## スキル

### web-scraper

URL のスクレイピング、ウェブサイトのクローリング、サイト URL のマッピング、ウェブ検索、構造化データの抽出、llms.txt の生成、複数 URL のバッチスクレイピング、またはウェブページの Markdown への変換を依頼した際に自動的に起動されます。直ちに firecrawl-operator エージェントに処理を委譲します。

## エージェント

### firecrawl-operator

ローカルの firecrawl API に対して `curl` + `jq` のシーケンスを実行する自律型ウェブスクレイピングエージェント。すべての v1 エンドポイントをカバーする 11 種類のプロトコルをサポートします。読み取り専用エージェント（Write、Edit、Agent ツールは使用しません）。

| プロトコル | エンドポイント | タイプ |
|----------|----------|------|
| HEALTH | `GET /` | 同期 |
| SCRAPE | `POST /v1/scrape` | 同期 |
| BATCH_SCRAPE | `POST /v1/batch/scrape` | 非同期 |
| CRAWL | `POST /v1/crawl` | 非同期 |
| CRAWL_CANCEL | `DELETE /v1/crawl/:id` | 同期 |
| CRAWL_ACTIVE | `GET /v1/crawl/active` | 同期 |
| CRAWL_ERRORS | `GET /v1/crawl/:id/errors` | 同期 |
| MAP | `POST /v1/map` | 同期 |
| SEARCH | `POST /v1/search` | 同期 |
| EXTRACT | `POST /v1/extract` | 非同期 |
| LLMSTXT | `POST /v1/llmstxt` | 非同期 |

## インフラストラクチャ

このプラグインは、開発コンテナ内で動作する firecrawl スタックが必要です。

| コンポーネント | ポート | 用途 |
|-----------|------|---------|
| Firecrawl API | 3002 | すべてのスクレイピング/クローリング/マッピング/検索/抽出エンドポイント |
| Playwright | 3000 | JavaScript レンダリングエンジン |
| Redis | 6379 | ジョブキューバックエンド |
| PostgreSQL | socket | クローリング/バッチジョブの永続化 |
| LiteLLM proxy | OPENAI_BASE_URL | extract 用 LLM バックエンド（オプション） |

スタックは `ENABLE_FIRECRAWL=true`（デフォルト）の場合に自動的に起動します。SessionStart フックが API に到達可能かどうかを確認し、サービスがダウンしている場合は警告を表示します。

## クラウド版 Firecrawl との違い

このプラグインはセルフホスト型オープンソースバージョンを使用します。

- スクレイピングに認証や API キーは不要
- クレジット制限やレート制限なし
- v1 API エンドポイントを使用（v2 ではない）
- ブラウザセッションおよびディープリサーチは利用不可
- extract はホスト型モデルの代わりに独自の LLM プロキシを使用
- ローカルコンテナネットワーク内で完全に動作
