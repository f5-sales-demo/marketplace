---
name: xorg-display
description: Safely observe and control Ubuntu 24.04 Xorg through xorgctl.
---

# Xorg display

Use only `xorgctl --json`. Follow observe, resolve, act, verify; acquire bounded input leases and return structured failures. Application behavior is declarative and uses bounded AT-SPI event observation.
